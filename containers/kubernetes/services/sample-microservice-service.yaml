apiVersion: v1
kind: Service
metadata:
  name: sample-microservice-service
  labels:
    app: sample-microservice
spec:
  selector:
    app: sample-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
